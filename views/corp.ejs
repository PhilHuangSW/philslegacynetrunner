<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= identity[0] %></title>

  <!-- BOOTSTRAP v5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">

  <!-- CUSTOM CSS -->
  <link rel="stylesheet" href="/stylesheets/home.css">

  <!-- GOOGLE FONTS ROBOTO FONT -->
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;400&display=swap" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Orbitron&display=swap" rel="stylesheet">

  <!-- FONT AWESOME -->
  <link rel="stylesheet" href="/stylesheets/all.css">

</head>


<body class="body bodyFade vh-100 overflow-auto">
  <%- include('./partials/navbar') %>
  <main class="container pt-5" id="mainView">
    <h1 class="text-white text-center pt-3">
      <%= deck_name %>
    </h1>

    <div class="row mb-3">


      <div class="col-sm-6">
        <!-- IDENTITY -->
        <h3 class="text-white">Identity:</h3>
        <div class="text-white">
          <a class=""><span id="<%= identity[0].id %>"><%= identity[0].title %></span></a>
        </div>


        <!-- EVENTS -->
        <h3 class="text-white">Events:</h3>
        <% for( let i = 0; i < event.length; i++ ) { %>
        <div class="text-white">
          <%= event[i].amount %>x
          <a class=""><span id="<%= event[i].id %>"><%= event[i].title %></span></a>
        </div>
        <% } %>


        <!-- RESOURCES -->
        <h3 class="text-white">Resources:</h3>
        <% for( let i = 0; i < resource.length; i++ ) { %>
        <div class="text-white">
          <%= resource[i].amount %>x
          <a class=""><span id="<%= resource[i].id %>"><%= resource[i].title %></span></a>
        </div>
        <% } %>
      </div>


      <div class="col-sm-6">
        <!-- HARDWARE -->
        <h3 class="text-white">Hardware:</h3>
        <% for( let i = 0; i < hardware.length; i++ ) { %>
        <div class="text-white">
          <%= hardware[i].amount %>x
          <a class=""><span id="<%= hardware[i].id %>"><%= hardware[i].title %></span></a>
        </div>
        <% } %>


        <!-- ICEBREAKERS -->
        <h3 class="text-white">Icebreakers:</h3>
        <% for( let i = 0; i < icebreaker.length; i++ ) { %>
        <div class="text-white">
          <%= icebreaker[i].amount %>x
          <a class=""><span id="<%= icebreaker[i].id %>"><%= icebreaker[i].title %></span></a>
        </div>
        <% } %>


        <!-- PROGRAMS -->
        <h3 class="text-white">Programs:</h3>
        <% for( let i = 0; i < program.length; i++ ) { %>
        <div class="text-white">
          <%= program[i].amount %>x
          <a class=""><span id="<%= program[i].id %>"><%= program[i].title %></span></a>
        </div>
        <% } %>
      </div>

      <!-- 36 Closing Row Div -->
    </div>


    <div class="row mb-3">
      <div class="col-sm-6">
        <img src="<%= allImages[identity[0].id] %> " alt="" class="img-fluid" id="img<%= identity[0].id %>">
        <% for( let i = 0; i < allImages.length; i++ ) { %>
        <img src="<%= allImages[i] %> " alt="" class="img-fluid d-none" id="img<%= i %>">
        <% } %>
      </div>

      <div class="col-6 text-white">
        <p><strong>Playstyle:</strong> Host every program onto <a
            href="https://netrunnerdb.com/en/card/13020">Dhegdheer</a> and <a
            href="https://netrunnerdb.com/en/card/25049">Dinosaurus</a> so that you have maximum MU for your high
          strength breakers, as well as maximum counters for <a
            href="https://netrunnerdb.com/en/card/05053">Overmind</a>.</p>
        <p><strong>Tips:</strong> Ideal rig would consist of <a href="https://netrunnerdb.com/en/card/01044">Magnum
            Opus</a> and <a href="https://netrunnerdb.com/en/card/13017">Adept</a> or <a
            href="https://netrunnerdb.com/en/card/13018">Savant</a> onto <a
            href="https://netrunnerdb.com/en/card/13020">Dhegdheers</a> with the other on <a
            href="https://netrunnerdb.com/en/card/25049">Dinosaurus</a>, 3x <a
            href="https://netrunnerdb.com/en/card/13015">LLDS Memory Diamond</a> and 3x <a
            href="https://netrunnerdb.com/en/card/07055">Data Folding</a> for a total of 9-12 strength breakers with 3
          credits at start of turn, and endless econ using <a href="https://netrunnerdb.com/en/card/01044">Magnum
            Opus</a>.</p>
      </div>
    </div>

    <%- include('./partials/footer') %>
  </main>





  <!-- JQUERY -->
  <script
    src="https://code.jquery.com/jquery-3.6.0.js"
    integrity="sha256-H+K7U5CnXl1h5ywQfKtSj8PCmoN9aaq30gDh27Xc0jk="
    crossorigin="anonymous">
  </script>

  <!-- BOOTSTRAP v5 -->
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous">
  </script>

  <script src="/javascripts/prompts.js"></script>

  <!-- CONTROLS WHICH IMAGES APPEAR ON SCREEN -->
  <script>
    const allImgs = <%- JSON.stringify(allImages) %>;
    for (let i = 0; i < allImgs.length; i++) {
      $('#' + i).hover(() => {
        for (let j = 0; j < allImgs.length; j++) {
          // console.log(j)
          $('#img' + j).addClass('d-none');
        }
        // console.log(i);
        $('#img' + i).removeClass('d-none');
      })
    }
  </script>

  <script>
    const x = window.matchMedia("(max-width: 900px)");

    function myFunc(x) {
      if (x.matches) {
        $('#mainView').removeClass('vh-100');
      } else {
        $('#mainView').addClass('vh-100');
      }
    };

    myFunc(x);
    x.addListener(myFunc);
  </script>

  <!-- FONT AWESOME -->
  <script defer src="/javascripts/all.js"></script>

</body>

</html>